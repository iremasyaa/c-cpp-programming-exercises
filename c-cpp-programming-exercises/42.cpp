#include <stdio.h>
#include <stdlib.h>
#include <time.h>

enum Yemek {Pizza,Hamburger,Döner,Lahmacun};
enum  tatli {Baklava, künefe};

enum Yemek rastgeleYemekSec();
enum Tatli rastgeleTatliSec();

int main() {
    srand(time(NULL));




    return 0;
}

enum Yemek rastgeleYemekSec() {






}

enum Yemek rastgeleTatliSec() {






}